@model IEnumerable<STGchannelMVC.Models.Language>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Language1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Language1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LangID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LangID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LangID })
        </td>
    </tr>
}

</table>
